name: Deploy RabbitConsumer

on:
  push:
    branches: [main]
    paths:
      - "RabbitConsumer/**" # Triggers ONLY when something in this folder changes

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    env:
      IMAGE_OWNER: ${{ github.repository_owner }}
      IMAGE_NAME: rabbit-consumer
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Verify Dockerfile Presence
        run: |
          if [ ! -f "./RabbitConsumer/Dockerfile" ]; then
            echo "Error: Dockerfile not found in RabbitConsumer directory!"
            ls -al ./RabbitConsumer
            exit 1
          fi

      - name: Print resolved GitHub environment values
        run: |
          echo "IMAGE_OWNER=${IMAGE_OWNER}"
          echo "IMAGE_NAME=${IMAGE_NAME}"
          echo "IMAGE_TAG=ghcr.io/${IMAGE_OWNER}/${IMAGE_NAME}:latest"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./RabbitConsumer
          file: ./RabbitConsumer/Dockerfile
          push: true
          platforms: linux/amd64
          tags: ghcr.io/${{ env.IMAGE_OWNER }}/${{ env.IMAGE_NAME }}:latest
